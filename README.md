React and Redux Universal Example
=================================

#### Introduction:
Just a simple demo showing how you can use React and Redux with universal rendering.

#### Install and run:
* `brew/apt-get install node`
* `npm install`
* `npm run develop`
